# Binaries
bin/
/usulnet
/usulnet-agent
*.exe
*.dll
*.so
*.dylib

# Build artifacts
tmp/
build/

# Test coverage artifacts
coverage.out
coverage.html

# Go test binaries — `go test -c` / `go test -bench` leave one of these
# under the package directory (e.g. tests/benchmarks/benchmarks.test).
*.test

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.production

# License private key - NEVER commit this
internal/license/keys/private.pem
