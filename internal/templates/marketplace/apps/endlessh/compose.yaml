services:
  endlessh:
    image: lscr.io/linuxserver/endlessh:1.1
    restart: unless-stopped
    ports:
      - "{{SSH_PORT}}:2222"
    environment:
      PUID: "1000"
      PGID: "1000"
      TZ: "Etc/UTC"
